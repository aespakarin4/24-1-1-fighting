#include <stdio.h>


int main() {
    int fahrenheit;

    printf("화씨온도를 입력하시오: ");
    scanf_s("%d", &fahrenheit);
    double celsius = 5.0 / 9.0 * (fahrenheit - 32);
    printf("섭씨 온도는 %.6f입니다.", celsius);

    return 0;
}
